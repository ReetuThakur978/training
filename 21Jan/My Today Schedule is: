My Today Schedule is:

1. Armstrong number
2. Sum of twodigit number
3. classes and objects
4. Varibles
5. Magic Functions construct(), destruct(), call(), get(), set(), isset(), unset(), sleep(), wakeup(), clone(), and __autoload().
6 Use of scope resolution operator
7 Use of "$this" and "self"
8.Namespace